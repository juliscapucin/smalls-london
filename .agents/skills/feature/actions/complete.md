# Complete Action

1. Stage all changes and commit with a descriptive message
2. Switch to main and merge the feature branch (no push yet)
3. Push main to origin ONCE (single push with all changes)
4. If feature branch was previously pushed, delete it from origin
